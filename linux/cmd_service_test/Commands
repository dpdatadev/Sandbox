Server commands:

C get_string function to return 1 - 2 string(s) of output 

System info:
UNAME -a -r

UUID for process identification

nmap for device detection

tcpdump (libcpap) for packet dumps

Custom commands:
PING (ICMP)
SMTP (Send email)
libcurl (HTTP)


